<!DOCTYPE html>
<html>
<head>
    <title>Masaru YouTube</title>
    <style>
        body { margin: 0; font-family: sans-serif; background: #23272a; color: white; display: flex; }
        .sidebar { width: 240px; background: #2c2f33; height: 100vh; position: fixed; }
        .nav-item { padding: 15px 25px; display: block; color: #b9bbbe; text-decoration: none; }
        .main { flex: 1; margin-left: 240px; padding: 40px; }
        .yt-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px; }
        .yt-card { background: #18191c; border-radius: 12px; overflow: hidden; border: 1px solid #444; }
        .yt-thumb { width: 100%; display: block; }
    </style>
</head>
<body>
    <div class="sidebar">
        <div style="padding: 25px; font-weight: bold; color: #5865f2; font-size: 20px;">MASARU BOT</div>
        <a href="/profile" class="nav-item">🏠 หน้าหลัก</a>
        <a href="/youtube" class="nav-item" style="background: #3c3f44; color: white;">🎥 วิดีโอ YouTube</a>
    </div>
    <div class="main">
        <h2 style="color: #ff0000;">🎥 ผลงานล่าสุดจาก Masaru</h2>
        <div class="yt-grid">
            <% videos.forEach(v => { %>
                <div class="yt-card">
                    <a href="https://www.youtube.com/watch?v=<%= v.id.videoId %>" target="_blank" style="text-decoration: none; color: white;">
                        <img src="<%= v.snippet.thumbnails.high.url %>" class="yt-thumb">
                        <div style="padding: 15px; font-size: 13px; font-weight: bold;"><%= v.snippet.title %></div>
                    </a>
                </div>
            <% }) %>
        </div>
        <div style="margin-top: 50px; text-align: center; border: 2px dashed #444; padding: 40px; border-radius: 20px;">
            <h3 style="color: #b9bbbe; margin: 0;">⏳ กำลังตัดต่อคลิปใหม่... รอติดตามเร็วๆ นี้!</h3>
        </div>
    </div>
</body>
</html>